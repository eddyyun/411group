USER STORY 1

Story: After logging in (Fail1), user wants to find out their date's mood. Ideally, user clicks a button which allows them to
take a photo (Fail2). User confirms the upload and the app will return their date's mood (Fail3).

Failure points:

  Fail1: login fail - reset password
  
  Fail2: Can't take a photo - Retry button and upload from device
  
  Fail3: App doesn't return mood - User sees error message, try take another photo


